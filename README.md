# Smart Loss Control (Monorepo)

## Structure
- frontend/ (synced from FE repo)
- backend/ (synced from BE repo)

## Local Setup
1) Copy env template:
   cp .env.example .env

2) Backend:
   cd backend
   npm install
   npm run dev

3) Frontend:
   cd ../frontend
   npm install
   npm run dev

